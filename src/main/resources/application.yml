# DataSource com.recSystem.Config

server:
  port: 8080
#  serverlet:
#    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/recsystem?characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  #原生配置
#  configuration:
#    #下划线转驼峰
#    map-underscore-to-camel-case: true